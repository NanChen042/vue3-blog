# Code Generator

> 根据自然语言描述，自动生成企业级模板代码。

## 支持的框架

- Vue 3 + Tailwind CSS
- React + Next.js
- Node.js + Express
- Golang + Gin

## 使用示例

在终端输入以下命令：
```bash
agent-cli generate "一个支持暗黑模式的用户登录页面"
```

## 模板自定义

如果你想修改生成的默认模板，可以在 `~/.agent/templates/` 目录下放置你的专属 `ejs` 模板。
